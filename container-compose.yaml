services:
  vmb-app:
    image: quay.io/swpm/vmb
    container_name: vmb
    ports:
      - 8003:8003
    volumes:
      - ./data:/data/vmb
    environment:
      - DEBUG=1
  vmb-mcp-app:
    image: quay.io/swpm/vmb-mcp
    container_name: vmb-mcp
    ports:
      - 8004:8080
    environment:
      # Crucial change: We now point to the 'django-web' service name on port 8000
      - DJANGO_API_URL=http://vmb-app:8003/api
      - PYTHONUNBUFFERED=1
    depends_on:
      - vmb-app
    stdin_open: true 
    tty: false
